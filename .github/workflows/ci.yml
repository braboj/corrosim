name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      - name: Install ruff + mypy + complexipy
        # complexipy is pinned to the minor: the snapshot file format is
        # version-sensitive (older snapshots are rejected across upgrades).
        run: |
          python -m pip install --upgrade pip
          pip install "ruff==0.15.*" "mypy==2.1.*" "complexipy==6.0.*"

      - name: Lint (ruff check)
        run: ruff check .

      - name: Type-check (mypy)
        # Non-strict; the scientific deps aren't installed in this job, so mypy
        # treats them as untyped via ignore_missing_imports (see [tool.mypy]).
        run: mypy

      - name: Cognitive complexity (complexipy)
        # Ratchet against the committed complexipy-snapshot.json watermark
        # ([tool.complexipy] in pyproject.toml): fails only when a function
        # over the threshold is new or has increased vs the snapshot.
        run: complexipy --color no

  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v5

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install package + dev deps
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run tests with coverage
        # Gated at 80% over the scoped surface: [tool.coverage.run] omit excludes
        # the QM-engine/Docker-only modules, and [tool.coverage.report] fail_under
        # enforces the threshold (issue #31 / ADR 0007).
        run: pytest -q --cov=corrosim --cov-report=term-missing

  security:
    name: SAST (bandit)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      - name: Install bandit
        run: |
          python -m pip install --upgrade pip
          pip install "bandit==1.8.*"

      - name: Bandit (SAST)
        run: bandit -c pyproject.toml -r src/corrosim

  secrets:
    name: Secret scan (gitleaks)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0        # full history so gitleaks scans all commits

      - name: gitleaks
        uses: gitleaks/gitleaks-action@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
