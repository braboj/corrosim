# Byte-compiled / caches
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.ipynb_checkpoints/

# Virtual envs
.venv/
venv/
env/

# Tool / run outputs
*.html
# curated multiscale report is tracked (see runs/make_report.py)
!report.html
*.cif
*.cube
*_slab.xyz
results.csv
!examples/*.csv

# QC scratch dirs
orca_*/
g16_*/
*.gbw
*.chk
*.log

# OS / editor
.DS_Store
.idea/
.vscode/
docs/whitepapers

# Local-only notes (kept with the project, never pushed)
*.local.md

# loose run-output figures in repo root (figures/ holds the curated set)
/*.png

# Volumetric cubes: large, regenerable (make_cubes) — never tracked
cubes/

# results/ holds tracked data (csv/json); keep transient preview PNGs out of it
results/*.png
