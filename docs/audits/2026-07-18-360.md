# 360-Degree Audit: 2026-07-18

Full-project review requested after the v0.4.0 release (dipole descriptor,
ADR-0034 independent-axis ranking, MD confinement, the #272 review epic) and the
two follow-up merges (#294 submodule bump, #295 playbook runbook), with the
backlog empty and the project reported at rest. Run per
`docs/solid-ai-templates/templates/base/workflow/360.md` as parallel,
context-isolated subagents. corrosim is a headless library and CLI (no
user-facing web surface), so the §360-headless adaptation applies: Value,
Viability, and Discovery are kept as light lenses, and the Quality perspective is
re-projected into six engineering dimensions (Architecture, Code Quality, Testing,
CI/CD, Security & Dependencies, Documentation). The overall grade is the lowest
dimension grade.

The mechanical layer was green before fan-out: `pytest -q` (325 test functions,
exit 0), scoped coverage 95.26% (gate 80%), `ruff check .` clean, `mypy` clean
(44 source files), `complexipy` clean (max cognitive complexity 11 against the 15
threshold; the `[]` snapshot is correct because no function is over threshold).
This is corrosim's first persisted 360 report; there was no prior `docs/audits/`
tree.

## Scores

| Dimension                     | Grade  | Findings | High+ |
| ----------------------------- | ------ | -------- | ----- |
| Value (user)                  | B+     | 11       | 0     |
| Architecture & Structure      | A      | 6        | 0     |
| Code Quality & API Contract   | A-     | 6        | 0     |
| Testing & Quality Gates       | A      | 5        | 0     |
| CI/CD & Tooling               | A-     | 6        | 0     |
| Security & Dependencies       | A-     | 6        | 0     |
| Documentation                 | B+     | 7        | 0     |
| Viability (analyst)           | A-     | 17       | 0     |
| Discovery (marketer)          | B      | 5        | 0     |
| **Overall**                   | **B**  | **69**   | **0** |

No finding is High or Critical severity. Every gate is green, the backlog is
empty, packaging and CI match the docs, and structure matches the ADRs. The
findings are uniformly minor or nit, and they cluster around one theme: small
documentation and tooling drift that accumulated in the days after v0.4.0 shipped
(a stale dev-journal resume line, pre-release caveats left in `user-journeys.md`,
a pre-commit exclude that no longer matches the ADR-0018 output paths). The single
most substantive item is a compliance obligation, not a defect: the published QM
Docker image redistributes two LGPL dependencies (`ase`, `tblite`) with no
third-party NOTICE file. Overall is B, set by Discovery, which is the inherently
limited marketing surface of a niche research tool rather than anything broken.
Discounting Discovery, the engineering and product body grades A-/B+.

## Issues created

Filed as #296-#307, grouped under epic #308, one issue per finding. Severities are
the audit's own; all are Minor or Nit. Grouped by theme, ascending effort.

Release-drift cleanup (Minor):

- F1 (#296). Refresh `docs/user-journeys.md`: journey 1 still calls the Pages showcase
  "planned (#68)" and journey 5 still warns `docker pull` returns "denied until the
  first release"; both were superseded by the live gallery and the shipped v0.4.0
  GHCR image. (`docs/user-journeys.md:34-35`, `:141`)
- F2 (#297). Add a dev-journal entry for the post-session-43 merges #294 and #295, and
  update the session-43 **Pending** line, which still proposes "bump the submodule"
  as optional-next although #294 already did it.
- F3 (#298). Re-anchor the pre-commit `exclude`: `results/.*` and `report/.*` are anchored
  at path root but ADR-0018 moved all outputs under `cases/<case>/...`, so they now
  match nothing; the text-based `cases/<case>/report/report.html` is therefore no
  longer skipped by `end-of-file-fixer`/`trailing-whitespace`. Change to
  `cases/.*/(results|report)/.*` (or add `.*\.html`). (`.pre-commit-config.yaml:9-10`)
- F4 (#299). Anchor `.gitignore` `results.csv` (line 33): the bare, unanchored name would
  silently ignore any file literally named `results.csv` anywhere, which contradicts
  the "`cases/<case>/results/*.csv` are tracked" invariant. Latent trap; no such
  file exists today.

Licensing and contribution hygiene (Minor):

- F5 (#300). Add a third-party NOTICE / licenses file and ship it in the QM image: `ase`
  (LGPL-2.1+) and `tblite` (LGPL-3.0+) are redistributed inside the published GHCR
  image, which carries a standard LGPL attribution obligation. This is the single
  most substantive item in the audit.
- F6 (#301). Add a `CONTRIBUTING.md` (and optionally a PR template): the git/review workflow
  is documented in CLAUDE.md and PLAYBOOK, but those are owner/agent-facing.

Convention fixes matching the project's own standards (Minor / Nit):

- F7 (#302). Add `Raises:` to `build_ensemble` (`src/corrosim/report/ranking.py:259`, raises
  `ValueError` on an empty molecule set) and to `build_adsorption_system`
  (`src/corrosim/adsorption/adsorption.py:81`, propagates `KeyError`/`ValueError` for
  an unknown metal). Both are ADR-0012 contract completeness that the docstring test
  does not check.
- F8 (#303). Capitalize the first word of ~15 comments that open with a lowercase prose word
  (§2.2 sentence-casing), clustered in `src/corrosim/presets.py` (178, 186, 192, 200,
  546, 552, 563), `src/corrosim/report/gallery.py` (106, 113, 191), and
  `src/corrosim/runs/run_study.py` (593, 598, 619, 686, 694). Not linter-caught.

Doc single-homing and optional polish (Nit):

- F9 (#304). De-duplicate the git-workflow ruleset stated near-verbatim in both CLAUDE.md
  §2.1 and PLAYBOOK §1 (commit trailer, branch-off-main, closes-#N warning,
  `*.local.md` rule); keep one home and cross-reference.
- F10 (#305). Surface `app.py` (the ADR-0030 unified dispatcher) in the README `src/corrosim/`
  structure row; ONBOARDING's key-files table already names it.
- F11 (#306). Optional: expose `corrosim.__version__` (via `importlib.metadata`) and a
  top-level `--version` flag; today the version lives only in `pyproject.toml`.
- F12 (#307). Optional: exclude `cubes/` and `cases/` in `.dockerignore` so a populated local
  tree does not inflate the build context (nothing wrong ships into the image today).

## Current bottleneck

Nominal lowest is **Discovery (B)**, but that reflects the inherently thin marketing
surface of a niche computational-chemistry CLI, not a defect: positioning is clear and
the tool is findable through GitHub plus the `braboj.me/corrosim` gallery. The real
actionable bottleneck is **Documentation (B+)** post-release drift plus the one
**licensing** obligation. Concretely: `user-journeys.md` still speaks in pre-release
future tense (F1), the dev-journal resume line is stale relative to #294/#295 (F2), and
the QM image redistributes LGPL binaries with no NOTICE file (F5). All three are cheap
to close in a single housekeeping change; none block use, distribution, or a further
release.

## 1. Value (user perspective): B+

Method: evaluated only user-facing materials (README "what it does", features, install,
usage; `docs/user-journeys.md`) with no access to developer docs, and spot-ran the
venv-safe CLI surface. Grade and findings adopted from the isolated Value reviewer.

Promises extracted: (1) screen any molecule by name or SMILES against a metal, end to
end; (2) rank candidates best-first with a transparent composite; (3) QM reactivity
descriptors (gap, hardness, ΔN) via xTB or DFT; (4) Fukui indices and ESP isosurfaces;
(5) Monte Carlo pose plus Brownian-dynamics RDF adsorption estimate; (6) one
self-contained HTML report with figures embedded; (7) free/open-source engines only,
Docker as the sole prerequisite; (8) `corrosim screen` fast triage; (9) `corrosim
run-study` full DFT to MC to MD bundle under `cases/<name>/`; (10) `corrosim
add-inhibitor <name|CAS>` from PubChem; (11) shipped validation cases across Fe/Cu/Al;
(12) callable as a Python library; (13) `--plan` engine-free dry run, substrates
Fe(110)/Cu(111)/Al(111); (14) live browser validation gallery.

| # | Sub-dimension | Status | Sev | Finding |
|---|---|---|---|---|
| 1 | Functional completeness (CLI) | PASS | - | `corrosim --help` and all three subcommand helps run; matches the README `corrosim <command>` model. |
| 2 | Functional completeness (report on disk) | PASS | - | `cases/arghel/report/` holds a real `report.html` (4.5 MB), `report.docx` (3.3 MB), 27 figures, 5 tables. A README-following user finds real output. |
| 3 | Self-contained report | PASS | - | report.html has zero external http/img references; 27 embedded base64 figures. Genuinely offline-openable as promised. |
| 4 | `--plan` / venv path | PASS | - | `screen --plan`, the `--inhibitors` shorthand, and `run-study --plan` run engine-free and print accurate step plans; the public Python API imports. |
| 5 | DFT/Docker claims | PARTIAL | - | Cannot run in a plain venv; clearly disclosed (no Windows wheels, so container), so documented-not-runnable, not a failure. |
| 6 | Content quality (README) | PASS | - | Accurate, deep, sourced; honest Limitations section; all referenced docs/files present. |
| 7 | Content currency (journeys) | PARTIAL | Nit | `user-journeys.md` carries stale pre-release caveats: showcase "planned (#68)" though the gallery is live; `docker pull` "denied until first release" though v0.4.0 shipped. (F1) |
| 8 | Usability (10-second clarity) | PASS | - | Title, tagline, and Features make the what/who obvious; Docker-only install is clear and copy-pasteable. |
| 9 | Docs match `--help` | PARTIAL | Nit | `user-journeys.md` mixes three invocation styles (`corrosim --inhibitors`, `corrosim-run-study`, `corrosim-add-inhibitor`) versus the README `corrosim <sub>` form; all work. Subcommand `--help` prints the console-script prog name, cosmetic. |
| 10 | Error/empty states | PASS | - | Documented CLI errors (unsupported metal, missing UFF param, unknown name, empty PubChem) are specific with exit codes; `--plan` previews engine-free. |
| 11 | Trust | PASS | - | Author and repo identified, MIT LICENSE present, CI/License/Python badges, PubChem source disclosed, issue tracker linked, latest commit today at the v0.4.0 tag. |

Summary: within seconds a first-time user can tell what corrosim is and run it; the CLI
matches the docs and the shipped arghel case has a real, fully self-contained report on
disk with no dead links. The only friction is that the secondary `user-journeys.md` has
drifted out of step with the shipped README, still speaking in pre-release future tense.
No blockers and nothing misrepresented, so B+.

## 2. Quality (engineer perspective), re-projected

Method: six context-isolated engineering reviewers plus live quality-gate runs. Each
dimension graded independently; the Quality contribution to the overall is the lowest of
these six.

### 2.1 Architecture & Structure: A

| # | Sub-dimension | Status | Sev | Finding |
|---|---|---|---|---|
| 1 | Structure matches ADRs | PASS | - | src/ layout with `qm/`, `adsorption/`, `report/`, `runs/` subsystem packages plus the facade files, per ADR 0011; the `corrosim <command>` front door per ADR 0030. |
| 2 | Entry points resolve | PASS | - | `corrosim=corrosim.app:main`, `corrosim-run-study=...run_study:main`, `corrosim-add-inhibitor=corrosim.fetch:main` all resolve to real targets. |
| 3 | Per-case output namespacing | PASS | - | Six cases each own `cases/<case>/{results,report}/` (ADR 0018/0019); drivers derive dirs from `--case`, no hardcoded output root. |
| 4 | Single source of truth | PASS | - | The case study lives once in `presets.ARGHEL`; drivers import it rather than re-declaring the molecule list. |
| 5 | No dead code / orphans | PASS | - | `from __future__` present in 42/44 modules (the two without it carry no annotations); no TODO/FIXME/commented-out blocks; backlog empty (0 issues, 0 PRs). |
| 6 | README structure map | PASS | Nit | The `src/corrosim/` structure row folds in the CLI but does not surface `app.py`, the ADR-0030 dispatcher; ONBOARDING's key-files table does. (F10) |

Grade rationale: structure is faithful to the ADRs with a clean subsystem split, resolving
entry points, and a working front door; one README-row nit holds it at a clean A.

### 2.2 Code Quality & API Contract: A-

| # | Sub-dimension | Status | Sev | Finding |
|---|---|---|---|---|
| 1 | Units in the contract | PASS | - | Every public quantity carries its unit in the name or docstring (`gap_ev`, `e_ads_kjmol`, `first_peak_metal_O`, `dipole_debye`); conversions named with both units. No bare numeric returns. |
| 2 | Metal-agnostic | PASS | - | The `metal` parameter is threaded through and the element is derived (`metal_element(metal)`), never assumed Fe; the Fe/Cu/Al dicts and `Fe(110)` defaults are the sanctioned uses. |
| 3 | Scientific-comment rule | PASS | - | Zero ticket/PR/ADR numbers in comments; non-obvious physics names its source (Koopmans, Rappé 1992, Lukovits ΔN, Yang-Mortier, Henderson-Hasselbalch, Tissandier). No "Stage N" leakage. |
| 4 | Comment placement | PASS | - | Comments sit above their item; the only right-of-code comments are the allowed tool directives (`# nosec`, `# noqa`). |
| 5 | `Raises:` completeness | PARTIAL | Minor | `build_ensemble` (`report/ranking.py:259`) raises `ValueError` with no `Raises:`; `build_adsorption_system` (`adsorption/adsorption.py:81`) propagates `KeyError`/`ValueError` undocumented. Invisible to the docstring test. (F7) |
| 6 | Comment sentence-casing | PARTIAL | Nit | ~15 comments open with a lowercase prose word (§2.2), clustered in `presets.py`, `report/gallery.py`, `runs/run_study.py`. Not linter-caught. (F8) |

Grade rationale: linter-clean, correctly metal-agnostic, and unit-disciplined; two
contract-completeness classes that slip past the gates (Raises, comment casing) pull it to
A-.

### 2.3 Testing & Quality Gates: A

| # | Sub-dimension | Status | Sev | Finding |
|---|---|---|---|---|
| 1 | Tests pass | PASS | - | `pytest -q` exit 0; 325 test functions; no skipped/xfail-without-reason surfaced. |
| 2 | Coverage measured and enforced | PASS | - | 95.26% against the 80% gate; QM-engine/Docker modules `omit`-ted per ADR 0007 scoped coverage; the two venv-runnable drivers are not omitted. |
| 3 | QM-light suite | PASS | - | No DFT/xTB/Docker in the pytest path, matching the deliberate fast-CI policy; the QM surface is exercised only in the container. |
| 4 | Complexity ratchet | PASS | - | `complexipy` clean; max cognitive complexity 11 versus the 15 threshold; the `[]` snapshot is correct (no over-threshold function to freeze). |
| 5 | Test naming / coverage of paths | PASS | - | One test module per unit (37 test files) covering descriptors, adsorption, report, runs, CLI, docstrings, goldens. |

Grade rationale: a fast, well-scoped suite at 95% coverage with all gates green and a
correctly functioning complexity ratchet. Straight A.

### 2.4 CI/CD & Tooling: A-

| # | Sub-dimension | Status | Sev | Finding |
|---|---|---|---|---|
| 1 | Gate coverage | PASS | - | All seven claimed gates are wired across `ci.yml` + `codeql.yml`: ruff (incl. D), mypy, complexipy, pytest+coverage, Bandit, gitleaks (full history), CodeQL. |
| 2 | Matrix | PASS | - | Python 3.10-3.12 on the test job; lint/security run single 3.12, matching the docs. |
| 3 | Publish pipelines | PASS | - | `release.yml`, `pages.yml`, `qm-image.yml` are ADR-backed (0022/0028/0024), path-gated, and free-tier. |
| 4 | Editor layer | PASS | - | `.vscode` + `.editorconfig` present; ADR 0033 defers editor type-checking to mypy. |
| 5 | Pre-commit exclude drift | PARTIAL | Minor | `results/.*` / `report/.*` are anchored at path root but ADR-0018 moved outputs under `cases/<case>/...`; the text `report.html` is no longer skipped by the whitespace hooks, risking spurious diffs on a machine-generated artifact. (F3) |
| 6 | Docker build context | PASS | Nit | `.dockerignore` excludes caches and `docs/local/` but not `cubes/`/`cases/`; only build-context size, nothing wrong ships into the image. (F12) |

Grade rationale: complete, layered, ADR-backed pipelines; the stale pre-commit exclude is
the one real (minor) tooling defect, so A-.

### 2.5 Security & Dependencies: A-

| # | Sub-dimension | Status | Sev | Finding |
|---|---|---|---|---|
| 1 | Secrets | PASS | - | No tokens, keys, private-key blocks, or absolute local paths outside the vendored submodule; only legitimate published author/trailer emails. |
| 2 | SAST + secret scan | PASS | - | Bandit, gitleaks, and CodeQL (weekly cron) all present; the two QM subprocess launches are `# nosec`-justified (fixed argv, no shell, no untrusted input). |
| 3 | Input boundaries | PASS | - | `add-inhibitor` uses stdlib `urllib` against public PubChem; no eval/shell-injection surface. |
| 4 | Dependency posture | PASS | - | Five lean core deps (numpy/rdkit/ase/pandas/matplotlib), cleanly split into `qm`/`viz`/`report`/`dev` extras; all actively maintained. |
| 5 | Dependency licenses | PARTIAL | Minor | Core deps are permissive (rdkit/pandas BSD, matplotlib PSF-style, numpy BSD), but `ase` (LGPL-2.1+, core) and `tblite` (LGPL-3.0+, `[qm]`) are weak-copyleft; see F5 for the redistribution obligation. |
| 6 | Reproducibility | PASS | - | Rebuildable from the repo alone: Dockerfile + compose define the QM env; results/reports regenerate from committed inputs. |

Grade rationale: a clean security posture (no secrets, three scanners, justified nosec) and
a lean dependency set; the LGPL attribution gap in the published image (F5) is the one
compliance item, so A-.

### 2.6 Documentation: B+

| # | Sub-dimension | Status | Sev | Finding |
|---|---|---|---|---|
| 1 | Doc homes | PASS | - | All ten homes from CLAUDE.md §1.4 exist at their stated paths. |
| 2 | ADR set integrity | PASS | - | `docs/decisions/` holds 0001-0034 contiguous, no gaps or duplicates; spot-checked ADRs are single-concern and well-formed. |
| 3 | Cross-reference integrity | PASS | - | Sampled ADR-number and file references across CLAUDE.md/README/ONBOARDING/PLAYBOOK all resolve; `LAMMPS_HANDOFF_NOTE` is a code constant, not a broken file link. |
| 4 | Dev-journal drift | PARTIAL | Minor | Latest entry is session 43; #294 (submodule) and #295 (playbook) merged after it with no entry, and the Pending line still proposes the submodule bump that #294 already did. (F2) |
| 5 | User-journeys currency | PARTIAL | Nit | Stale pre-release caveats remain (F1); same drift as Value finding 7. |
| 6 | Single-homing | PARTIAL | Nit | The git-workflow ruleset is near-verbatim in CLAUDE.md §2.1 and PLAYBOOK §1; the verify-command block repeats across README/ONBOARDING/CLAUDE. (F9) |
| 7 | ADR cosmetics | PASS | Nit | The `Upstream:` line convention starts at ADR 0019 (0001-0018 lack it, harvested via the know-how channel); ADR 0005/0020 fold away the standalone Consequences heading; header style is a uniform house style. |

Grade rationale: the documentation body is genuinely strong (homes intact, ADRs contiguous,
cross-references resolve), but it carries the largest share of real, if minor, findings, all
post-v0.4.0 drift; B+.

## 3. Viability (business-analyst perspective): A-

Method: isolated business-analyst reviewer over LICENSE, `pyproject.toml`, workflows, and
the operational docs. Grade and findings adopted from that reviewer.

| # | Sub-dimension | Status | Sev | Finding |
|---|---|---|---|---|
| 1 | License clearly stated | PASS | - | Full MIT text in LICENSE, matching `pyproject` license field + OSI classifier + README. |
| 2 | Runtime deps MIT-compatible | PARTIAL | Minor | Most permissive, but `ase` (LGPL-2.1+) and `tblite` (LGPL-3.0+) are weak-copyleft and are redistributed inside the GHCR image with no NOTICE/third-party-license file. (F5) |
| 3 | License fits intended use | PASS | - | MIT on a free-software-only OSS scientific tool. |
| 4 | Privacy surface | PASS | - | Local compute tool, reads no secrets, needs no `.env`; `add-inhibitor` hits public PubChem only. None. |
| 5 | Hosting/infra cost | PASS | - | No server; GHCR + Pages + Actions are free-tier for public repos. |
| 6 | Paid service / vendor lock-in | PARTIAL | Nit | All-in on GitHub; lock-in low and reversible (portable CI YAML, OCI image, static Pages) but the migration path is not written down. |
| 7 | Pipeline cost | PASS | - | Free tier; path-gated image/pages workflows; release fires only on `v*` tags. |
| 8 | Onboard from docs alone | PASS | - | README + ONBOARDING + PLAYBOOK + pipeline/validation give a complete path. |
| 9 | Bus-factor (solo) | PARTIAL | Nit | Single owner, no named successor, but unusually well-mitigated by 34 ADRs, the dev-journal, the MEMORY index, and the CI stack. |
| 10 | Visible direction | PASS | - | Empty backlog intentional and documented; direction lives in ADRs + validation scorecards. |
| 11 | Maintenance burden | PASS | - | No hosted service; QM-light CI; regenerable artifacts; only chore is the drift-guarded manual QM-image rebuild. |
| 12 | Contribution model | PARTIAL | Nit | Workflow documented but owner/agent-facing; no `CONTRIBUTING.md`/PR template/code of conduct. (F6) |
| 13 | Single-maintainer dep risk | PARTIAL | Nit | QM extras are smaller academic projects, but optional and behind the ADR-0015 engine abstraction, so one engine's loss is survivable. |
| 14 | Deps actively maintained | PASS | - | Core deps and QM engines release within 12 months as of knowledge cutoff. |
| 15 | Dependency count minimal | PASS | - | Five core deps; fetch uses stdlib `urllib`. Lean. |
| 16 | Survive dep abandonment | PASS | - | Engine backends pluggable (xTB/PySCF/ORCA/Gaussian); core deps foundational or replaceable. |
| 17 | Disaster recovery | PASS | - | Rebuildable from the repo alone; no external state, no secrets, no manual config beyond one-time GHCR/Pages toggles noted in workflow comments. |

Summary: a healthy, low-liability project, zero infra cost, a lean and well-partitioned
dependency set, fully reproducible from the repo, and a documentation/ADR trail that
materially softens the solo bus-factor. The one substantive item is the LGPL bundling
attribution gap (F5); lighter gaps are the absent `CONTRIBUTING.md` and the undocumented
GitHub-exit path. A-.

## 4. Discovery (marketer perspective): B

Method: light lens, parent-assessed. The §360-headless guidance notes Discovery largely does
not apply to a headless CLI, so this is a brief treatment of the surface that does exist (the
public repo, the README, and the Pages gallery), not a full isolated reviewer. Several
sub-dimensions (analytics, OG social cards, conversion funnels) are N/A for a niche research
tool and are not counted against the grade.

| # | Sub-dimension | Status | Sev | Finding |
|---|---|---|---|---|
| 1 | Positioning | PASS | - | It is immediately clear what corrosim is and who it is for (green corrosion-inhibitor screening for researchers); the tagline states the value. |
| 2 | Findability | PARTIAL | Nit | Discoverable via GitHub search and the `braboj.me/corrosim` gallery, but audience is niche; reach rests mostly on those two channels. |
| 3 | README as a shop window | PASS | - | Badges under the title, a Docker one-liner up top, a live gallery link, and a scannable feature list make a compelling first impression for the target reader. |
| 4 | Social proof | PARTIAL | Nit | Minimal (stars/testimonials/citations) as expected for a young single-author scientific tool; the validation gallery is the strongest credibility signal. |
| 5 | Distribution | PASS | - | Present through at least two independent channels (GitHub repo + GHCR image + the Pages domain); content is indexable static HTML. |

Grade rationale: positioning and first impression are strong and the tool is genuinely
findable, but the marketing surface and social proof are inherently thin for a niche
research CLI; B. This dimension is strategic, not broken, and is not a use-, distribution-,
or release-blocker.

## Method & mechanical layer

- Executed per `docs/solid-ai-templates/templates/base/workflow/360.md`, §360-headless
  adaptation. Nine role-isolated lenses: three engineering reviewers (docs consistency;
  CI/git/deps/security; code conventions) plus live gate runs feeding the six re-projected
  Quality dimensions, and three dedicated subagents for Value, Viability, and (light)
  Discovery. Each engineering and product finding was cross-checked against the actual files
  before inclusion.
- Gates run live from a clean working tree on `main` at v0.4.0 (`git describe`:
  v0.4.0-3-g3461e12): pytest exit 0 (325 test functions), coverage 95.26% (gate 80%), ruff
  clean, mypy clean (44 files), complexipy clean (max 11 / threshold 15). Submodule in sync
  at solid-ai-templates v2.17.0-131-g19facb5. Backlog empty (0 open issues, 0 open PRs).
- Note on scope: corrosim's convention chain (CLAUDE.md header) does not currently list
  `workflow/360.md`, and the SYS-07 smoke check that enforces the `docs/audits/` location
  lives in the submodule, not in corrosim's test suite. This report adopts the template's
  location and format regardless. Wiring the convention into CLAUDE.md and adding the
  end-of-session "append the dated report" step is an optional follow-up.

<!-- Generated with solid-ai-templates (github.com/braboj/solid-ai-templates) -->
